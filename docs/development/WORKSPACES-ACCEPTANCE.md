# 项目工作区验收对照

核对日期：2026-09-10。用户明确批准后，正式服务器已完成切换及生产工作流验收。表中继续标明各项证据来自预览还是生产，避免把预览故障演练写成正式数据上的演练。服务端验证记录保留在私有部署目录中，文档只引用记录名，不复制账号凭据或研究正文。

需求来源是本轮确认的统一模型：用户拥有项目，项目拥有唯一文件区及一条或多条对话；上传和 Agent 产物进入同一文件区，左侧每个项目提供资源／对话视图，右侧查看和编辑文件。完整操作说明见 [工作区开发指南](WORKSPACES.md)。

## 模型、导航和文件操作

| 要求 | 已核对的实现与证据 | 当前结论及范围 |
| --- | --- | --- |
| 首次发送或上传才建立文件区及首条对话；不存在“对话升级项目”流程 | 草稿提交接口、`test_workspace_drafts.py`、`verify-drafts.py`；实际连续新建、未发送输入和空白刷新 | 新规则正式页面通过；仅浏览空白页不持久化项目，原持久模型仍只有一种项目实体 |
| 同项目新增对话共享文件；不同项目不共享文件 | HTTP 多线程项目检查；`workspace-uploaded-formats-verification.json` 从第二对话运行并读取首个对话所属项目的五种文件；实际容器隔离记录 | 预览通过；新增对话不复制文件区 |
| 对话消息和模型选择分别保存，不自动合并其他对话历史 | 网页工作流记录核对两条原生聊天的各自消息；`bridge.py` 仅传当前请求消息；broker 会话范围包含用户、项目、线程、模型；Worker 使用全新的私有状态并关闭全局记忆 | 原生消息独立及运行边界已核对；没有声称捕获过供应商收到的完整请求 |
| 默认使用首条对话名称；手动改名后不被自动标题覆盖 | `test_title_and_thread_delete_preserve_workspace`；真实 HTTP 的 `manual_title_thread_deletion_and_project_recovery`；网页第二对话工作流保留手动项目名 | 预览通过 |
| 删除对话保留文件；删除项目有恢复策略 | 同上；项目删除 API 返回 `recoverable`，原生消息仍保留，项目回收站可恢复 | 预览通过；不自动销毁版本 |
| 恢复最近线程和文件查看状态；后台查询不改变导航 | `workspace-uploaded-formats-verification.json` 实际 GET、POST、运行快照前后检查；账号切换、恢复页面和网页刷新记录 | 显式打开才改变最近线程；预览恢复已验证 |
| 每个项目都有资源／对话视图；打开文件不离开当前对话 | `WorkspaceSidebar.svelte`、`state.ts`；实际网页文件卡片、双线程及右侧渲染记录 | 预览通过 |
| 可创建目录／文件、改名、移动、删除、下载；稳定 ID 不因改名移动变化 | 文件 API 验收、`workspace-file-move-verification.json`；稳定引用和目录归属测试 | 预览通过；Markdown 相对引用的移动刷新也已验证 |
| 同名上传不静默覆盖，编辑冲突保留双方结果 | 存储重复上传及版本测试；HTTP 冲突检查；`workspace-save-race-verification.json` 实际保存等待期间继续输入和切换文件 | 预览通过 |

## 文件流转与呈现

| 要求 | 已核对的实现与证据 | 当前结论及范围 |
| --- | --- | --- |
| 原始 Markdown、PDF、图片、ZIP 上传后仍可读取、下载 | `workspace-uploaded-formats-verification.json`：实际 HTTP 上传和下载，实际 Hermes 终端读取五类原件；PDF 文本提取、PNG 解码、GIF 两帧、ZIP 条目及内容核对，全部哈希不变 | 预览通过；证明原件读取，不冒充模型视觉理解质量评估 |
| ZIP 显式解包，限制路径、链接、体积及数量，失败不发布半套文件 | 上述记录证明 ZIP 不自动解包；`verify-api.py` 显式解包；存储路径穿越、配额和事务回滚检查 | 预览通过 |
| Agent 真正生成文件后才登记、显示卡片并可下载 | `workspace-runtime-verification.json`、`workspace-browser-workflow-verification.json`：真实 DeepSeek 文件工具读写、回执、消息持久化及网页卡片；多格式检查另覆盖终端产物登记 | 预览通过，覆盖通用文件与终端渠道 |
| Markdown 预览、编辑、版本保存与未保存草稿 | 网页编辑、保存竞态和恢复页面记录 | 预览通过 |
| PDF、图片／GIF、CSV、源码及移动端查看 | `workspace-media-verification.json` 核对 PDF 画布、图片缩放、CSV 引号／换行、相对引用、390×844 全屏；源码通过文本编辑器呈现 | 预览通过；不承诺 Office 编辑或新增 TeX 编译 |
| 大文件、未知格式及渲染失败明确降级，保留原件下载 | `workspace-preview-fallbacks-verification.json`（07:02:03 UTC）实际检查大文本、未知二进制、损坏 PNG/PDF 的提示；逐个从页面下载并核对原件字节 | 新版预览通过，镜像 `8db0b33d118e…` |
| 不可信内容不在主页面执行脚本 | Markdown 渲染去除危险标签；媒体页面记录确认脚本元素被移除；上述降级记录确认 HTML 原文显示在源码编辑器，预览区无 script／iframe／object／embed；鉴权内容响应携带 sandbox 和 nosniff | 页面与响应头检查通过 |
| CFD 产物与 LaTeX 工程进入受控工作区 | 扩展记录生成并登记 PNG、四帧 GIF 和 LaTeX ZIP；LaTeX 真实主模型／辅助模型运行及既有 ZIP 复核；`workspace-cfd-browser-verification.json`（07:04:24 UTC）另记录真实模型完成三个 CFD MCP 调用，PNG/GIF 卡片持久化、下载解码、右侧渲染及刷新保留 | 扩展与文件登记通过，CFD 实际网页交付已补齐；本轮不承诺 TeX 编译服务 |
| 取消、失败、重启不发布半成品、不损坏版本 | 新版真实 Docker 生命周期记录；恢复页面；提交、快照和启动取消竞态测试；完整发布事务回滚测试 | 预览通过；未完成输出需用户明确恢复到独立目录 |

## 隔离、迁移与运维

| 要求 | 已核对的实现与证据 | 当前结论及范围 |
| --- | --- | --- |
| 所有文件操作根据登录身份鉴权，文件 ID 不是访问密钥 | 双账号实际 HTTP 的列表、内容、版本、编辑、移动、删除拒绝；原生附件引用归属检查 | 预览通过 |
| 文件工具、终端及其子进程不能访问其他用户／项目文件 | 真实 Docker 验收：唯一工作副本挂载、非 root、只读系统、移除 capabilities、禁止提权、无 Docker socket；跨用户和同用户跨项目文件不可读；宿主正对照下容器间网络仍拒绝 | 运行边界已验证 |
| MCP、插件不靠共享进程全局切换用户目录 | Worker 独立运行 MCP／插件，只有当前项目挂载；实际 stdio 工具注册和调用；CFD 输出路径与 LaTeX 持久工程改造 | 统一容器边界及功能调用已验证；不声称枚举测试了每个第三方工具的全部参数组合 |
| 公开回复、进度、错误、文件卡片及导出不交付宿主路径 | 全分片路径过滤测试；真实模型回复；真实历史卡片及浏览器 JSON 导出；原生文件上传／列表／搜索／详情／改名实际 API 验收 | 对应公开交付渠道通过；用户自己输入的原文及文件内容不改写 |
| 账号切换时清空缓存、预览和在途操作状态，迟到结果不串账号 | `workspace-account-switch-verification.json`：真实数据库锁延迟上传，A 退出／B 登录，B 不显示 A 的状态或文件，迟到上传归 A，B 使用 ID 仍被拒绝 | 真实整页登录切换通过 |
| 历史对话按原用户迁移；只给有证据的产物建立映射 | 离线迁移检查、预览和正式迁移；正式原账号归属与原件哈希；真实所属账号 HTTPS 页面及导出 | 正式迁移通过：6 个原账号、20 条聊天、1 个原生附件、6 个有证据产物；16 个归属不明文件留在管理员隔离区 |
| 配额、版本保留、执行副本清理与备份策略 | 历史配额含删除内容和同用户其他项目；原子失败检查；新版提交后崩溃清理；[维护说明](WORKSPACES.md#容量保留与维护) | 已实现固定配额和保留策略；没有自动删除历史或清理失败副本的定时任务 |
| 回退保留上线后新增文件、版本和消息，维持隔离 | 实际预览镜像往返切换，检查点后新增数据及页面卡片保留；三个发布故障检查；三个首次迁移故障检查 | 预览回退及失败边界通过；不允许把旧共享 Agent 当作回退目标 |
| 完成正式迁移、关闭旧共享入口、部署并让用户试用 | `workspace-production-verification.json`、`workspace-production-browser-workflow-verification.json`；实际 HTTPS 双线程模型生成、上传／预览／刷新；旧进程与端口检查 | **已完成。正式入口已切换，旧共享 Agent 停用并禁止启动；临时账号、密钥与模型授权清理完毕** |
| 旧服务停止后模型仍可发现并实际使用 | `workspace-model-discovery-verification.json`；上游静态 `model_ids` 配置；正式真实模型工作流 | 9 个科研模型目录可列出，隐藏旧基座；个人密钥与可用性仍独立检测，默认选中可用模型已验证 |

## 记录解释

39 项 Linux 工作区测试针对源码 `03f5ff45af2139bf` 已通过，新增旧网关以失败退出状态停止时的真实进程／端口判定检查。文件原件读取、历史导出和新版容器生命周期也有对应实际记录。测试替身覆盖的故障处理不等于真实服务故障演练；普通图片渲染不等于特定插件产物已经经过实际聊天交付。

前端完整构建通过；全量类型检查仍有 7701 项错误、204 项警告。固定上游基线为 7789 项错误，当前工作区组件没有新增诊断；余下三项对比文本差异位于既有回复组件的文件／反馈类型。此状态不能写成全量类型检查通过，也不能用构建成功代替页面验收。

本轮范围内的实现、预览验证、正式迁移及生产工作流验收已经完成。正式镜像为 `8db0b33d118e…`，调度器源码为 `03f5ff45af2139bf`；完整部署位置、备份和检查点见 [正式部署记录](WORKSPACES.md#本次正式部署记录)。真实生产页面确认历史卡片和 JSON 导出可用，两个新线程的实际模型文件交付及刷新持久化通过；没有导入预览数据库。

仍适用的能力边界：不提供 Office 文档完整编辑或新增 TeX 编译服务；原件可读取不等于评估了模型的视觉理解质量；前端全量类型检查保留上述上游基线问题。失败／中断副本和版本按文档保留，不自动销毁。


侧栏与草稿后续更新已在正式镜像 `10ac63aa079f…` 验收，Linux 工作区测试增至 42 项。正式草稿工作流与数据保留记录见 [侧栏与草稿更新](WORKSPACES.md#2026-09-10-侧栏与草稿更新)。上文 `8db0b33d118e…` 对应首次工作区上线记录。
